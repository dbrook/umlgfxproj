# Ignore compiled object files
*.o

# Ignore any qmake/Eclipse OS-dependent oddities
.cproject
.project
finalproject.pro.user
Makefile
Makefile.Release
Makefile.Debug

# Make clean won't destroy this executable so don't sync it
finalproj.exe

# These get generated by qmake to allow signals and slots to
# work properly. They may as well be system dependent, too.
moc_glwidget.cpp
moc_window.cpp
